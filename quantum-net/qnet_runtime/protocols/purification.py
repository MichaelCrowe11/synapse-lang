"""entanglement purification: roadmap placeholder, not an implementation."""

STATUS = "roadmap"
NAME = "purification"


def run(*args, **kwargs):
    """Refuse explicitly. An empty module used to import as if it were a feature."""
    raise NotImplementedError("purification: roadmap protocol: no validated implementation, no security or fidelity claim (docs/trinity-validation.md, gate 4)")
