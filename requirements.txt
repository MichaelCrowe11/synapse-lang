# Core runtime dependencies. pyproject.toml is the source of truth; this file
# mirrors [project].dependencies for tools that only read requirements.txt.
# Optional extras (jit, quantum, cloud, enterprise, dev, docs, jupyter) are
# declared in pyproject.toml under [project.optional-dependencies].
numpy>=1.24.0
scipy>=1.10.0
sympy>=1.12
matplotlib>=3.7.0
colorama>=0.4.6
networkx>=3.0
pandas>=2.0.0
